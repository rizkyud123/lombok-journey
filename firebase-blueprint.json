{
  "entities": {
    "AppData": {
      "collection": "app_data",
      "fields": {
        "businessInfo": {
          "type": "map",
          "description": "Information about Lombok Journey business including phone, email, WhatsApp, and social media"
        },
        "services": {
          "type": "array",
          "description": "List of tour packages and travel services"
        },
        "destinations": {
          "type": "array",
          "description": "List of tourist destinations in Lombok"
        },
        "galleryActivities": {
          "type": "array",
          "description": "List of trip activities and documented photos with watermark"
        },
        "estimatorConfig": {
          "type": "map",
          "description": "Pricing calculation rules and available destinations for the trip estimator"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO timestamp of the latest change"
        },
        "version": {
          "type": "number",
          "description": "Incremental version number"
        }
      }
    }
  }
}
